language: python
python:
  - '3.8.2'
  # - 'nightly' # test builds take a long time, best used before new Python releases
dist: bionic # latest
install: pip install -e '.[lint]'
script:
  - ssc-pkg --help # TODO add unit tests for main.py
  - ./rtest.sh
